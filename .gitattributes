# Auto detect text files and perform LF normalization
* text=auto

# Batch files are parsed by cmd.exe, which is far happier with CRLF. Forcing it
# here makes setup.bat behave the same even with core.autocrlf=false.
*.bat text eol=crlf
*.cmd text eol=crlf
